package com.jspider.thread;

public class ThreadMethods {
	
	t.start()-->to thread execution
	public void run()--> logic inside a method inherit from thread
	t.sleep()-->to wait the given m=seconds
	t.join()-->waiting for next thread execution 
	t.currentThread()--> to check the current thread like([#1,main,5,main])
	t.getName()--->return the values 
	t.setName()--->assign the values
	t.setPriority()--->set the thread priority
	t.getPriority()--->get the thread priority
	}